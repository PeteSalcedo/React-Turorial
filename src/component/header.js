import react from "react"
import React from "react"
import logo from "./react-logo.png"
function Header() {
    return(
        <header>
            <nav>
                <img src={logo} />
             
            </nav>
        </header>
    )
}

export default Header